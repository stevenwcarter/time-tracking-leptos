//! Placeholder import prompt.
//!
//! Task 24 replaces this with the real banner offering to import a
//! signed-out user's local entries once they sign in.

use leptos::prelude::*;

/// Offers to import locally-stored entries after signing in.
#[component]
pub fn ImportBanner() -> impl IntoView {
    view! { <div class="hidden">"Import banner arrives in Task 24."</div> }
}
